{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table text-light">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_season_index') }}" class="text-light">back to list</a>

    <a href="{{ path('app_season_edit', {'id': season.id}) }}" class="text-light">edit</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
